/**
 * PricingWithUnits provides a price table element for the passed products.
 * It is for products which have per unit pricing with unit tiers.
 * @returns The pricing element.
 */
const PricingWithUnits = ({ productName, productData, unitName }) => {
  return (
    <>
      <h2>{productName} Pricing</h2>
      <div></div>
    </>
  )
}

export default PricingWithUnits
